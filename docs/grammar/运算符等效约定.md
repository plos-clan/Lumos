
# 运算符等效约定

我们约定以下运算符等效

`a = a + b` 与 `a += b`<br>
`a = a - b` 与 `a -= 1`<br>
`a = a * b` 与 `a *= b`<br>
`a = a / b` 与 `a /= b`<br>
*依此类推*

`a++` 与 `a += 1`<br>
`a--` 与 `a -= 1`<br>

`a if expr else b` 与 `expr ? a : b`<br>
`a and b` 与 `a && b`<br>
`a or b` 与 `a || b`<br>
`not a` 与 `!a`<br>
