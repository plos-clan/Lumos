
# 命名节点 `Named` : I `AST`

- `Root` 根节点
- `Container` 父节点
- `str` 节点的名称

## 具名函数 `NamedFunc` : `Named`

- `Vector<Function>` 重载列表

## 类型引用 `TypeRef`

- `Type` 节点的引用

## 函数引用 `FuncRef`

- `Func` 节点的引用

## 变量引用 `VarRef`

- `Var` 节点的引用
