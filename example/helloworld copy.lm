use "std" -> std;

@assert(argc >= 1)
act[io.out] main(i32 argc, [[]string] argv) -> i32 {
  std.println("Argc is 1.") if argc == 1;
  for (var i32 a = 1; a < argc; a++) {
    std.println("Hello, world!");
  }
  return 0;
}
